<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.assetmanagement.backend.mapper.BoardMapper">

    <!-- Posts -->
    <select id="findAllPosts" resultType="com.assetmanagement.backend.entity.BoardPost">
        SELECT p.*, u.name as authorName, u.email as authorEmail
        FROM board_post p
        JOIN users u ON p.author_id = u.id
        <if test="search != null and search != ''">
            WHERE p.title LIKE CONCAT('%', #{search}, '%')
            OR p.content LIKE CONCAT('%', #{search}, '%')
        </if>
        ORDER BY p.created_at DESC
    </select>

    <select id="findPostById" resultType="com.assetmanagement.backend.entity.BoardPost">
        SELECT p.*, u.name as authorName, u.email as authorEmail
        FROM board_post p
        JOIN users u ON p.author_id = u.id
        WHERE p.id = #{id}
    </select>

    <insert id="insertPost" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO board_post (title, content, author_id, created_at)
        VALUES (#{title}, #{content}, #{authorId}, CURRENT_TIMESTAMP)
    </insert>

    <update id="updatePost">
        UPDATE board_post
        SET title = #{title},
            content = #{content},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <delete id="deletePost">
        DELETE FROM board_post WHERE id = #{id}
    </delete>

    <update id="incrementViewCount">
        UPDATE board_post SET view_count = view_count + 1 WHERE id = #{id}
    </update>

    <!-- Comments -->
    <select id="findCommentsByPostId" resultType="com.assetmanagement.backend.entity.BoardComment">
        SELECT c.*, u.name as authorName, u.email as authorEmail
        FROM board_comment c
        JOIN users u ON c.author_id = u.id
        WHERE c.post_id = #{postId}
        ORDER BY c.created_at ASC
    </select>

    <select id="findCommentById" resultType="com.assetmanagement.backend.entity.BoardComment">
        SELECT * FROM board_comment WHERE id = #{id}
    </select>

    <insert id="insertComment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO board_comment (post_id, author_id, content, created_at)
        VALUES (#{postId}, #{authorId}, #{content}, CURRENT_TIMESTAMP)
    </insert>

    <delete id="deleteComment">
        DELETE FROM board_comment WHERE id = #{id}
    </delete>

</mapper>
